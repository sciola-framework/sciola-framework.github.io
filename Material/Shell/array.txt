# ------------------------------------------------------------------------------
# Loop - Command [ for ]
# ------------------------------------------------------------------------------
LIST=(A B C D E F)

for i in ${!LIST[*]};do
  echo ${LIST[$i]}
done
# ------------------------------------------------------------------------------
